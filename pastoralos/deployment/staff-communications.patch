--- a/supabase/functions/pastoralos-staff/index.ts
+++ b/supabase/functions/pastoralos-staff/index.ts
@@ -1,4 +1,5 @@
-import { twilioConnection } from '../../../lib/pastoral/twilio-connection.ts';
+import {readBroadcastConfig,deliveryConnection} from '../../../lib/pastoral/broadcast-worker.ts';
+import { twilioAccount, twilioConnection } from '../../../lib/pastoral/twilio-connection.ts';
 import { handleEdgeStaff } from '../../../lib/pastoral/edge-staff.ts';
 import { staffApiDependencies } from '../../../lib/pastoral/staff-api.ts';
 import { runtimeSecrets } from '../../../lib/pastoral/runtime-secrets.ts';
@@ -11,6 +12,7 @@
       PASTORALOS_SUPABASE_SECRET_KEY: keys.default || Deno.env.get('SUPABASE_SERVICE_ROLE_KEY'),
     });
     const secrets = runtimeSecrets(name => Deno.env.get(name));
+    deps.deliveryConnection = ()=>deliveryConnection(readBroadcastConfig(name=>Deno.env.get(name),secrets.twilioAuthToken?{accountSid:twilioAccount.accountSid,phone:twilioAccount.phone,token:secrets.twilioAuthToken}:undefined));
     deps.connection = connect => twilioConnection(secrets.twilioAuthToken,connect);
     return await handleEdgeStaff(request,deps);
   } catch {
--- a/lib/pastoral/edge-staff.ts
+++ b/lib/pastoral/edge-staff.ts
@@ -4,7 +4,7 @@
 // Authorize each request through Supabase Auth and the live staff membership.
 export function handleEdgeStaff(request: Request, deps: StaffApiDependencies) {
   const path = new URL(request.url).pathname;
-  const match = path.match(/^\/(?:functions\/v1\/)?pastoralos-staff\/(session|state|action|import|people-import|greeting|greeting\/reset|twilio|twilio\/connect)$/);
+  const match = path.match(/^\/(?:functions\/v1\/)?pastoralos-staff\/(delivery-connection|session|state|action|import|people-import|greeting|greeting\/reset|twilio|twilio\/connect)$/);
   if (!match) return Response.json({error: 'Not found.'}, {status:404});
   const origin = request.headers.get('Origin');
   if (origin && origin !== 'https://1ag.tv') return Response.json({error: 'Untrusted request.'}, {status:403});
--- a/lib/pastoral/staff-api.ts
+++ b/lib/pastoral/staff-api.ts
@@ -1,3 +1,4 @@
+import {applyCommunicationsAction,type DeliveryConnection} from './communications.ts';
 import { approvePrayerRequest } from './prayer-approval.ts';
 import { GREETING_ROOT, readGreetingAudio, validateGreetingAudio, type VoiceGreeting } from './greeting.ts';
 import { importPeopleCsv } from './people-csv.ts';
@@ -8,6 +9,7 @@
 
 type Staff = { email: string; workspaceOwner: string };
 export interface StaffApiDependencies {
+  deliveryConnection?():DeliveryConnection;
   uploadGreeting?(bytes:Uint8Array):Promise<string>;
   removeGreeting?(url:string):Promise<void>;
   connection?(connect: boolean): Promise<unknown>;
@@ -47,7 +49,7 @@
 
 export async function handleStaffApi(request: Request, deps: StaffApiDependencies) {
   const path = new URL(request.url).pathname.replace(/^\/pastoralos/, '');
-  if (!['/api/session','/api/state','/api/action','/api/import','/api/people-import','/api/twilio','/api/twilio/connect','/api/greeting','/api/greeting/reset'].includes(path)) return json({ error: 'Not found.' }, 404);
+  if (!['/api/delivery-connection','/api/session','/api/state','/api/action','/api/import','/api/people-import','/api/twilio','/api/twilio/connect','/api/greeting','/api/greeting/reset'].includes(path)) return json({ error: 'Not found.' }, 404);
   if (request.method !== (['/api/action','/api/import','/api/people-import','/api/twilio/connect','/api/greeting','/api/greeting/reset'].includes(path) ? 'POST' : 'GET')) return json({ error: 'Method not allowed.' }, 405);
   const origin = request.headers.get('Origin');
   if ((origin && origin !== new URL(request.url).origin) || request.headers.get('Sec-Fetch-Site') === 'cross-site') return json({ error: 'Untrusted request.' }, 403);
@@ -56,6 +58,7 @@
   try {
     const staff = await deps.authenticate(authorization.slice(7));
     if (!staff) return json({ error: 'This account does not have PastoralOS access.' }, 403);
+    if(path === '/api/delivery-connection')return json(deps.deliveryConnection?.()??{sms:false,email:false});
     if (path === '/api/session') return json({ email: staff.email });
     if (path.startsWith('/api/twilio')) {
       if (!deps.connection) return json({error:'Twilio setup is unavailable.'},503);
@@ -85,7 +88,7 @@
       catch(e){if(e instanceof Error&&e.message==='CONFLICT')await deps.removeGreeting?.(url);throw e;}
     }
     if (request.headers.get('Content-Type')?.split(';')[0].trim() !== 'application/json') return json({ error: 'JSON required.' }, 415);
-    const limit = ['/api/import','/api/people-import'].includes(path) ? 2000000 : 30000;
+    const limit = ['/api/import','/api/people-import'].includes(path) ? 2000000 : 150000;
     const reader = request.body?.getReader();
     const chunks: Uint8Array[]=[];let size=0;
     if(reader)for(;;){const {done,value}=await reader.read();if(done)break;size+=value.byteLength;if(size>limit){await reader.cancel();return json({error:'This request is too large.'},413);}chunks.push(value);}
@@ -110,7 +113,9 @@
     if (data.version !== current.version) return json({ error: 'This workspace changed. Refresh before saving.' }, 409);
     let state: State;
     try {
-      if(data.action.type==='prayer.approve-send') {
+      if(data.action.type.startsWith('directory.')||data.action.type.startsWith('broadcast.')){
+        state=applyCommunicationsAction(current.state,data.action,staff.email,deps.deliveryConnection?.()??{sms:false,email:false});
+      }else if(data.action.type==='prayer.approve-send') {
         state=approvePrayerRequest(current.state,data.action,staff.email);
       } else if(data.action.type==='automation.retry'||data.action.type==='automation.review') {
         const next=structuredClone(current.state) as AutomationState;
--- a/lib/pastoral/model.ts
+++ b/lib/pastoral/model.ts
@@ -145,7 +145,7 @@
   const duplicate=s.people.find(p=>p.id!==existing?.id&&!p.archived&&((email&&p.email===email)||(normalized&&p.phone===normalized)));
   if(duplicate&&!a.allowShared)throw Error(`This contact matches ${duplicate.name}. Open that record or confirm this is a separate person with a shared contact.`);
   const stage=existing?.stage??'New guest';
-  const p:Person={id:existing?.id??uid(),name,email,phone:normalized,address:validText(v.address??'',500),household:validText(v.household??'',100),stage,paused:existing?.paused??false,archived:existing?.archived??false,prayerMember:isBool(v.prayerMember??false),guestSms:isBool(v.guestSms??false),guestEmail:isBool(v.guestEmail??false),prayerSms:isBool(v.prayerSms??false),firstVisit:existing?.firstVisit??at,stageEnteredAt:existing?.stageEnteredAt??existing?.firstVisit??at,source:existing?.source??'Staff entry',note:validText(v.note??'',2000),context:(existing?.context??0)+1,sample:existing?.sample??false,completedAt:existing?.completedAt};
+  const p:Person={...existing,id:existing?.id??uid(),name,email,phone:normalized,address:validText(v.address??'',500),household:validText(v.household??'',100),stage,paused:existing?.paused??false,archived:existing?.archived??false,prayerMember:isBool(v.prayerMember??false),guestSms:isBool(v.guestSms??false),guestEmail:isBool(v.guestEmail??false),prayerSms:isBool(v.prayerSms??false),firstVisit:existing?.firstVisit??at,stageEnteredAt:existing?.stageEnteredAt??existing?.firstVisit??at,source:existing?.source??'Staff entry',note:validText(v.note??'',2000),context:(existing?.context??0)+1,sample:existing?.sample??false,completedAt:existing?.completedAt};
   if(existing){s.people[s.people.indexOf(existing)]=p;s.messages.filter(m=>m.targets.some(t=>t.personId===p.id)&&m.program==='guest'&&m.status!=='cancelled').forEach(m=>change(m,s,'held'));s.deliveries.filter(d=>d.target.personId===p.id&&d.status==='queued').forEach(d=>{d.status='suppressed';d.reason='Person or permissions updated';});}
   else {s.people.unshift(p);if(a.enroll!==false){const r=s.rules.find(r=>r.id==='welcome'&&r.enabled);if(r)makeDraft(s,p,r,at);}}
   label=`${existing?'Updated':'Added'} ${name}; contact permissions recorded`;
